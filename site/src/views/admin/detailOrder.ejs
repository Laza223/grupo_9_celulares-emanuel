<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head", { title: "Productos en database" }) %>



    <body class="dashboard-body">
        <%- include("./navBar") %>
            <main class="admin-main-list">
                <div class="admin-header-list">
                    <div class="admin-header-list-title">
                        <h3></h3>
                        <h2>Detalle de orden</h2>
                    </div>
                </div>

            
                    <br>
                 
                        
                 
                                  
                 <br>
          
          
                     
                       
                           
                    <br>
                    <br>
                         
                          

<br><div class="container">
    <div class="container">
       
           
            
                <h2>Orden N° <%= order.id %></h2>
          
 
     
        <div class="row">
          <div class="col-md-12">
            <table class="table">
              <thead>
                <tr>
                  <th>Imagen</th>
                  <th>Nombre</th>
                  <th>Cantidad</th>
                  <th>Precio</th>
                </tr>
              </thead>
              <tbody>
                <% order.products.forEach(product => { %>
                  <tr>
                    <td><img src="/images/products/<%= product.image %>" class="img-product-admin" alt="<%= product.name %>"></td>
                    <td><%= product.name %></td>
                    <td>
                      <% const orderProduct = product.orderProducts; %>
                      <% if (orderProduct) { %>
                        <%= orderProduct.quantity %>
                      <% } else { %>
                        0
                      <% } %>
                    </td>
                    <td><%= product.price.toLocaleString('es-AR', { style: 'currency', currency: 'ARS' }) %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
                          
                
          </div>      
          
          
          <div class="card  <%= order.state === "pending" ? " text-bg-warning mb-3" : order.state === "completed" ? " text-bg-success mb-3" : "text-bg-secondary mb-3" %>" id="order-detail-head" style="width: 18rem;">
              <div class="card-body">
                <h5 class="card-title"> Estado : <%= order.state %></h5>
                <h6 class="card-subtitle mb-2 "> Usuario: <%= order.user.name %> <%= order.user.surname %>  </h6>
                <p class="card-text"></p>
                <a href="#" class="card-link"></a>
                <h2>Total : <%= order.total.toLocaleString('es-AR', { style: 'currency', currency: 'ARS' }) %></h7>
              </div>
            </div>
              
          
              

            </main>


            <%- include("../partials/scripts") %>

    </body>

</html>